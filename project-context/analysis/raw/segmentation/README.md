# Segmentation output

**File:** `waitlist_segmented_2026-02-21.csv`

**Source:** Waitlist `Become an Agentic Architect_waitlist_1904contacts_022026.csv` with HCP score and segment assigned per `project-context/plans/segmentation.md`.

**Columns added:** `hcp_score` (0–100), `segment`, `days_since_last_activity`.

**Segments:** Urgent | High Intent | Engaged Nurture | Cold Nurture | Reactivation | Unsubscribed (exclusion).

**Note:** Per-lead email opens/clicks are not in raw data, so the email-engagement component of HCP is 0 for all leads. HCP is based on behavioral intent (source), profile completeness, and recency (LL signup, cart, webhook events). For a final DM push, filter to `segment` in (Urgent, High Intent) and `status != enrolled`.

**Generated by:** `scripts/run_segmentation.py`

---

**File:** `urgent_high_intent_2026-02-21.csv`

**Source:** Filter of `waitlist_segmented_2026-02-21.csv` to rows with `segment` = Urgent or High Intent. `linkedin_url` is filled where possible from the same segmented data and from the cart export (same email may have LinkedIn in cart when missing in waitlist); URLs are normalized to `https://`.

**Use:** Shortlist for 1:1 DM outreach. Exclude rows with `status` = enrolled. Only a subset of leads have `linkedin_url` populated from internal data; the rest can be looked up manually or via a tool.

**Generated by:** `scripts/filter_urgent_high_intent.py`

**Personalized messages (last-24h push):** The same file includes columns added by `scripts/add_personalized_messages.py`:

- **`channel`** — `LinkedIn DM` (send the message as a LinkedIn DM) or `Email` (send as personal email).
- **`email_subject`** — For Email channel only; suggested subject line.
- **`personalized_message`** — Copy-paste message per lead. Angles used: Top 5 trending course on Maven, Cohort 2 Wall of Fame (2 projects), deadline Sun Feb 22 noon, last cohort at $1,500 (then $1,995), LL25 for Lightning Lesson participants. Message varies by source (Started payment / Lightning Lesson / Waitlist / Lead magnet) and channel.

**Exclusion:** One lead (Saurabh Bhardwaj — aryan.saurabhbhardwaj@gmail.com) is marked `[DO NOT CONTACT]` per campaign plan; filter or skip that row when sending.
